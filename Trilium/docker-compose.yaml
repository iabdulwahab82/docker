services:
  trilium:
    image: zadam/trilium
    restart: always
    environment:
      - TRILIUM_DATA_DIR=/home/node/trilium-data
    ports:
      - "8086:8080"
    volumes:
      - /volume/docker/trilium:/home/node/trilium-data
